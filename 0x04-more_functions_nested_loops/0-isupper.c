#include "main.h"
/**
 *_isupper -check if upper
 *@c: input to function
 *Return: 1 if alpha 0 if not
 */

int _isupper(int c)
{
	if (isupper(c) != 0)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

